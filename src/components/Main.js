import * as React from 'react';

const Main = (props) => {
    return (
        <div>
            <p>
            Hello {props.name} Edit <code>src/App.js</code> and save to reload.
            </p>
        </div>
    );
};

export default Main;